" #######################################
"
" SrcExpl 的配置
"
" #######################################

" 視窗高度
let g:SrcExpl_winHeight = 8

" 更新時間 單位是 ms
let g:SrcExpl_refreshTime = 100

" "Enter" 按鍵會跳到定義位置
let g:SrcExpl_jumpKey = "<ENTER>" 

" "Space" 按鍵會返回跳轉前的位置
let g:SrcExpl_gobackKey = "<SPACE>" 

" 依照關鍵字顯示本地定義
let g:SrcExpl_searchLocalDef = 1 

" 不要在開啟 SrcExpl 時更新 tags 檔案
let g:SrcExpl_isUpdateTags = 0 

